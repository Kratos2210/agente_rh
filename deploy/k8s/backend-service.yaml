apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: agente-rh
  labels:
    app.kubernetes.io/name: agente-rh
    app.kubernetes.io/component: backend
spec:
  selector:
    app.kubernetes.io/name: agente-rh
    app.kubernetes.io/component: backend
  ports:
    - port: 8000
      targetPort: 8000
